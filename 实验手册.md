


# 1. **安装工具链**
```bash
# 一次安装所有必需工具
sudo apt update

# RISC-V C 编译器（核心！）
# 汇编器、链接器等
# 多架构调试器（支持 RISC-V）
# QEMU 模拟器（运行 xv6）
# 构建工具
# 版本控制
sudo apt install \
    gcc-riscv64-unknown-elf \
    binutils-riscv64-unknown-elf \
    gdb-multiarch \
    qemu-system-misc \
    make \
    git     
```

# 2. **验证安装**
```bash
# 检查编译器
riscv64-unknown-elf-gcc --version

# 检查调试器
gdb-multiarch --version | head -1

# 检查 QEMU
qemu-system-riscv64 --version
```

# 3. 第一章总结
- 递归下降解析器， 抽象语法树--shell 命令解析
- IO重定向， 文件描述符--偏移量
- 管道
- 文件系统， inode， fstate

# 4. 实验步骤

## 1. sleep
- Makefile
- user/sleep.c



## 2. pingpong
- Makefile
- user/pingpong.c


## 3. primes
- Makefile
- user/primes.c

## 3. find
- Makefile
- user/find.c


## 实验笔记：静态缓冲区的陷阱

### 函数定义
```c
char* fmtname(char *path)
{
  static char buf[DIRSIZ+1];  // 静态缓冲区 - 陷阱所在！
  char *p;

  // 找到最后一个斜杠后的第一个字符
  for(p = path + strlen(path); p >= path && *p != '/'; p--)
    ;
  p++;  // 指向文件名开始位置

  // 返回空白填充的名字
  if(strlen(p) >= DIRSIZ)
    return p;
  
  memmove(buf, p, strlen(p));                // 复制文件名
  memset(buf + strlen(p), ' ', DIRSIZ - strlen(p));  // 剩余部分用空格填充
  return buf;  // 返回静态缓冲区的地址
}
```

### 问题分析

**主要问题：**
- 使用 `static char buf[DIRSIZ+1]` 作为返回缓冲区
- 所有调用共享同一个内存区域
- 后续调用会覆盖之前的结果

### 演示代码
```c
#include <stdio.h>
#include <string.h>

#define DIRSIZ 14

char* fmtname(char *path) {
  /* 上面的代码 */
}

int main() {
  // 第一次调用 - 正常
  char* name1 = fmtname("/home/user/file1.txt");
  printf("文件名1: [%s]\n", name1);  // 显示: file1.txt
  
  // 第二次调用 - 覆盖了第一次的结果
  char* name2 = fmtname("/home/user/document.pdf");
  printf("文件名2: [%s]\n", name2);  // 显示: document.pdf
  printf("再次打印文件名1: [%s]\n", name1);  // 也显示: document.pdf !!!
  
  // 第三次调用
  char* name3 = fmtname("/tmp/data");
  printf("文件名3: [%s]\n", name3);
  
  // 现在所有指针都指向相同的内容
  printf("\n所有指针都指向相同的内存:\n");
  printf("name1地址: %p, 值: [%s]\n", (void*)name1, name1);
  printf("name2地址: %p, 值: [%s]\n", (void*)name2, name2);
  printf("name3地址: %p, 值: [%s]\n", (void*)name3, name3);
  
  // 危险的使用方式
  printf("\n危险示例 - 同时保存多个结果:\n");
  char* names[3];
  names[0] = fmtname("/dir/a.txt");
  names[1] = fmtname("/dir/b.txt");
  names[2] = fmtname("/dir/c.txt");
  
  printf("所有文件应该不同，但实际上:\n");
  for(int i = 0; i < 3; i++) {
    printf("names[%d]: [%s]\n", i, names[i]);
    // 所有三个都会显示 "c.txt" 或类似的结果
  }
  
  return 0;
}
```

### 运行结果示例
```
文件名1: [file1.txt    ]
文件名2: [document.pdf ]
再次打印文件名1: [document.pdf ]  ← 被覆盖了！

所有指针都指向相同的内存:
name1地址: 0x404040, 值: [data         ]
name2地址: 0x404040, 值: [data         ]
name3地址: 0x404040, 值: [data         ]

危险示例 - 同时保存多个结果:
names[0]: [c.txt        ]
names[1]: [c.txt        ]
names[2]: [c.txt        ]
```

### 问题根源

1. **静态缓冲区共享**：`static` 关键字使 `buf` 在程序生命周期内只有一份
2. **返回局部静态变量的指针**：每次调用都返回同一个地址
3. **多次调用相互影响**：最后一次调用决定所有返回值的内容

### 解决方案

#### 方案1：调用者提供缓冲区
```c
void fmtname2(char *path, char *buf, size_t bufsize) {
  char *p;
  for(p = path + strlen(path); p >= path && *p != '/'; p--)
    ;
  p++;
  
  if(strlen(p) >= bufsize)
    strncpy(buf, p, bufsize-1);
  else {
    strcpy(buf, p);
    memset(buf + strlen(p), ' ', bufsize - strlen(p) - 1);
  }
  buf[bufsize-1] = '\0';
}
```

#### 方案2：动态分配内存
```c
char* fmtname3(char *path) {
  char *p;
  for(p = path + strlen(path); p >= path && *p != '/'; p--)
    ;
  p++;
  
  char *buf = malloc(DIRSIZ + 1);
  if(!buf) return NULL;
  
  if(strlen(p) >= DIRSIZ) {
    strncpy(buf, p, DIRSIZ);
    buf[DIRSIZ] = '\0';
  } else {
    strcpy(buf, p);
    memset(buf + strlen(p), ' ', DIRSIZ - strlen(p));
    buf[DIRSIZ] = '\0';
  }
  
  return buf;  // 调用者需要free
}
```

#### 方案3：线程安全版本（使用线程局部存储）
```c
char* fmtname4(char *path) {
  static __thread char buf[DIRSIZ+1];  // 每个线程有自己的副本
  // ... 其余代码相同
}
```

### 实验总结

**教训：**
- 返回静态缓冲区指针的函数不能保存多个返回值
- 使用时必须立即处理结果，不能保存指针供后续使用
- 在多线程环境中，这种模式会导致数据竞争

**适用场景：**
- 结果立即使用且不需要保存
- 单次调用或链式调用（`printf("%s", fmtname(path))`）
- 性能要求高且能接受此限制

**最佳实践：**
1. 优先让调用者提供缓冲区
2. 使用动态分配时，文档中明确说明需要释放内存
3. 避免在需要保存多个结果的场景使用静态缓冲区

这个 `fmtname` 函数是一个经典的教科书案例，展示了静态缓冲区的便利性和危险性。


# **非纯函数害人不浅**

`fmtname` 函数是一个典型的**非纯函数（Impure Function）**，它违反了函数式编程的重要原则，在实际开发中埋下了许多隐患：

## 🚨 主要问题

### 1. **隐蔽的副作用**
```c
// 看起来无害的调用
char* name1 = fmtname("/a.txt");
char* name2 = fmtname("/b.txt");

// 实际效果：name1 和 name2 指向相同内存
printf("%s == %s ? %s\n", name1, name2, 
       name1 == name2 ? "相同地址!" : "不同地址");
// 输出: "b.txt == b.txt ? 相同地址!"
```

### 2. **违反直觉的行为**
函数签名 `char* fmtname(char *path)` 暗示：
- 输入一个路径
- 返回一个格式化后的文件名
- **但实际上**：返回的是静态缓冲区的地址，内容会被后续调用覆盖

## 💥 真实场景的危害

### 场景1：批量处理文件
```c
// 错误的方式 - 所有结果都一样！
char* results[100];
for(int i = 0; i < 100; i++) {
    results[i] = fmtname(files[i]);  // 每次都覆盖同一个缓冲区
}
// results[0]...results[99] 都指向最后一个文件的名字
```

### 场景2：多线程灾难
```c
// 线程A
void* thread_a(void* arg) {
    char* name = fmtname("/home/a/data.txt");
    sleep(1);  // 切换线程
    printf("A: %s\n", name);  // 可能输出B的文件名！
}

// 线程B  
void* thread_b(void* arg) {
    char* name = fmtname("/home/b/config.cfg");
    printf("B: %s\n", name);
}

// 结果不可预测，存在数据竞争
```

### 场景3：调试噩梦
```c
char* name1 = fmtname("/tmp/log.txt");
debug_function();  // 内部调用了 fmtname
// name1 的内容被意外修改了！

char* name2 = fmtname("/var/data.bin");
// 这里断点调试时，name1 和 name2 会显示相同内容
```

## 📊 问题根源分析

### 非纯函数的特征：
- ❌ 有隐藏的内部状态（静态缓冲区）
- ❌ 相同输入不一定得到相同输出（取决于调用历史）
- ❌ 调用顺序影响结果
- ❌ 不能安全地缓存结果

### 函数签名误导：
```c
// 签名暗示这是纯函数：
// - 只依赖于输入参数
// - 没有副作用
// - 可重复调用
char* fmtname(char *path);

// 但实际上这是带状态的函数
char* fmtname(char *path, static_buffer_state);
```

## 🔧 改进方案

### 方案A：纯函数版本（推荐）
```c
// 调用者管理内存，无副作用
void fmtname_pure(const char* path, char* output, size_t size) {
    // 不依赖任何静态状态
    // output 由调用者提供
}
```

### 方案B：明确的状态传递
```c
// 明确显示函数依赖外部状态
struct Formatter {
    char buffer[DIRSIZ+1];
};

char* fmtname_with_state(struct Formatter* fmt, const char* path) {
    // 操作 fmt->buffer，状态对外可见
}
```

### 方案C：不可变返回值
```c
// 返回新分配的内存，调用者负责释放
char* fmtname_alloc(const char* path) {
    char* result = malloc(DIRSIZ+1);
    // ... 填充数据
    return result;  // 每次都是新的内存
}
```

## 📝 设计原则总结

### 应该避免的模式：
```c
// 模式1：返回静态缓冲区
static char buf[SIZE];
return buf;

// 模式2：修改全局变量
global_var = process(input);
return &global_var;

// 模式3：依赖上一次调用的结果
static int counter = 0;
return counter++;
```

### 应该遵循的原则：
1. **明确性**：函数的副作用应该在签名中明确
2. **可预测性**：相同输入总是得到相同输出
3. **独立性**：函数调用不依赖历史状态
4. **线程安全**：可安全地在多线程环境中使用

## 🎯 实际教训

```c
// 曾经困扰无数开发者的经典陷阱
char* strerror(int errnum);  // 标准库中的类似函数

// 错误用法：
char* err1 = strerror(EINVAL);
char* err2 = strerror(ENOMEM);
// err1 和 err2 可能指向相同字符串！

// 正确用法：
printf("Error1: %s\n", strerror(EINVAL));
printf("Error2: %s\n", strerror(ENOMEM));
// 或者立即复制到自己的缓冲区
```

## 💡 结论

**非纯函数如同程序中的"地雷"**：
- 看似简单高效，实则隐患重重
- 违反最小惊讶原则
- 导致难以调试的问题
- 阻碍代码复用和并行化

**建议**：
- 优先设计纯函数
- 如必须有状态，明确化
- 在文档中清晰说明副作用
- 对返回指针的函数保持警惕

记住：**代码的清晰性比微小的性能优化更重要**。一个容易理解、行为可预测的函数，远比一个高效但充满陷阱的函数更有价值。

---
*"调试一段有副作用的代码，就像在雷区里找钥匙——你不知道哪一步会引爆问题。"*


## 4. find
- Makefile
- find.c



## 附赠一个非纯函数bug
```c
#include <stdio.h>
#include <string.h>

const char* file1 = "document.orz";
const char* file2 = "report.txt";
const char* file3 = "data.pdf";

char* get_suffix(const char* filename) {
    static char suffix[4];
    
    // 根据文件名直接判断
    if (strcmp(filename, "document.orz") == 0) {
        strcpy(suffix, "orz");
    } else if (strcmp(filename, "report.txt") == 0) {
        strcpy(suffix, "txt");
    } else if (strcmp(filename, "data.pdf") == 0) {
        strcpy(suffix, "pdf");
    } else {
        strcpy(suffix, "");
    }
    
    return suffix;
}

int main() {

    printf("检查文件后缀...\n");
    
    char* suffix1 = get_suffix(file1);
    printf("%s -> 后缀: %s\n", file1, suffix1);
    
    char* suffix2 = get_suffix(file2);
    printf("%s -> 后缀: %s\n", file2, suffix2);
    
    printf("\n比较 file1 和 file2:\n");
    if (strcmp(suffix1, suffix2) == 0) {
        printf("❌ %s 和 %s 后缀相同\n", file1, file2);
    } else {
        printf("✅ %s 和 %s 后缀不同\n", file1, file2);
    }
    
    printf("\n比较 file2 和 file3 (有问题的部分):\n");
    // 看起来只调用了一次 get_suffix，但实际上....
    char *suffix3 = get_suffix(file3);
    
    printf("%s -> 后缀: %s\n", file3, suffix3);
    printf("%s -> 后缀: %s\n", file2, suffix2);
    
    // 再次比较，但这次会得到错误的结果
    printf("\n比较 file2 和 file3:\n");
    if (strcmp(suffix2, suffix3) == 0) {
        printf("❌ %s 和 %s 后缀相同\n", file2, file3);
    } else {
        printf("✅ %s 和 %s 后缀不同\n", file2, file3);
    }
    

    printf("\n=== 检查后缀 ===\n");
    printf("%s -> 后缀: %s\n", file2, suffix3);
    printf("%s -> 后缀: %s\n", file3, suffix3);
    
    return 0;
}

```